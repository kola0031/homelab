apiVersion: v1
kind: pod
metadata:
  name: prometheus
  namespace: default
  labels:
    app: prometheus
spec:
  containers:
  - name: prometheus
    image: prom/prometheus:latest
    ports:
    - containerPort: 9090
