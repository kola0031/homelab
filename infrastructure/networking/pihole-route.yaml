apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: pihole
  namespace: pihole
spec:
  parentRefs:
  - name: homelab-gateway
    namespace: monitoring
  hostnames:
  - "prometheus.local"
  rules:
  - backendRefs:
    - name: pihole
      port: 80